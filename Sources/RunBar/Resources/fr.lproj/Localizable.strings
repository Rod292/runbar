/* RunBar — French localization disabled.
 *
 * The previous content has been intentionally cleared so that French-locale
 * macOS users still see the English UI (Bundle.module falls back to the
 * development region — en — when a key is missing here).
 *
 * To re-enable French, restore the prior keys from git history:
 *   git log -p -- Sources/RunBar/Resources/fr.lproj/Localizable.strings
 */
