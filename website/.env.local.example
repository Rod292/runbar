# Strava OAuth — server-side secret used by /api/strava/exchange and refresh.
# Get values from https://www.strava.com/settings/api.
# Copy this file to .env.local for dev; set the same vars on Vercel for prod.

STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
