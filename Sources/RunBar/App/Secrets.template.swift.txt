// Copie ce fichier en `Secrets.swift` (sans le .txt) puis remplis les valeurs.
// Ce fichier-ci est un template — `Secrets.swift` est gitignored.
//
// 1. Crée une app Strava : https://www.strava.com/settings/api
//    - Authorization Callback Domain : runbar (n'importe lequel — on intercepte côté ASWebAuthenticationSession)
// 2. Copie ID et secret ici.

import Foundation

enum Secrets {
    static let stravaClientID     = "<TON_CLIENT_ID>"
    static let stravaClientSecret = "<TON_CLIENT_SECRET>"
    static let stravaRedirectURI  = "runbar://oauth"
    static let stravaScope        = "activity:read_all"
    static let webhookVerifyToken = "<OPTIONNEL>"
}
